<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.epicodus.bogglesoltaire.MainActivity"
    android:background="#000000">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/imageView"
        android:src="@drawable/background"
        android:scaleType="centerCrop" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Boggle Soltaire!"
        android:layout_marginTop="126dp"
        android:textColor="@color/colorTextIcons"
        android:id="@+id/boggleSolitaireTextView"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/playBoggleButton"
        android:layout_alignStart="@+id/playBoggleButton" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/rollDiceEditText"
        android:background="@color/colorTextIcons"
        android:layout_above="@+id/playBoggleButton"
        android:layout_marginBottom="10dp"
        android:padding="5dp"
        android:hint="Roll Dice!"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Play!"
        android:id="@+id/playBoggleButton"
        android:background="@color/colorAccent"
        android:textColor="@color/colorTextIcons"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
